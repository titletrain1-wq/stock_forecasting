# stock_forecasting

Personal single-user forecast app for US stocks and crypto.

## Quick Start

```bash
uv sync
uv run python worker.py  # terminal 1
uv run streamlit run app.py  # terminal 2
```

See `docs/` for architecture and implementation notes.
